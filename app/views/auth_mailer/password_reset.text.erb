Hi <%= @user.name %>,

We received a request to reset your password for <%= @shop.name %>.
Use the link below within <%= (Users::Credentials::RESET_TOKEN_TTL / 60).to_i %> minutes:

<%= @reset_url %>

If you didn't request this, you can ignore this email.

– MomGo Support
